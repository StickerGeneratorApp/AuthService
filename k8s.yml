apiVersion: apps/v1
kind: Deployment
metadata:
  name: "AuthService"
spec:
  selector:
    matchLabels:
      app: "AuthService"
  template:
    metadata:
      labels:
        app: "AuthService"
    spec:
      containers:
        - name: "AuthService"
          image: "gcr.io/AuthService/jib-image"
          ports:
            - name: http
              containerPort: 8080
          readinessProbe:
            httpGet:
              path: /health/readiness
              port: 8080
            initialDelaySeconds: 5
            timeoutSeconds: 3
          livenessProbe:
            httpGet:
              path: /health/liveness
              port: 8080
            initialDelaySeconds: 5
            timeoutSeconds: 3
            failureThreshold: 10
---
apiVersion: v1
kind: Service
metadata:
  name: "AuthService"
spec:
  selector:
    app: "AuthService"
  type: LoadBalancer
  ports:
    - protocol: "TCP"
      port: 8080